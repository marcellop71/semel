[package]
name = "semel-sys"
version = "0.1.0"
edition = "2021"
description = "Raw FFI bindings to libsemel (persistent cohomology)"
publish = false

[dependencies]
libc = "0.2"

[build-dependencies]
